# 安全说明

VeloTradeX 涉及真实资金的下单与交易所密钥，安全问题优先级很高。我们重视社区上报的漏洞，并会尽快响应。

## 报告漏洞

请**不要**在公开 issue 中透露可被利用的漏洞细节。建议通过以下方式私密报告：

- 通过 GitHub issues 设置「私密 / security」标签后进行报告（若仓库支持私密 issue）；
- 或直接联系维护者（见 `package.json` 中的 author 邮箱）。

请在报告中尽量包含：

- 影响版本与复现步骤；
- 漏洞类型与可能造成的危害（如资金损失、权限提升、信息泄露）；
- 复现所需的配置（如使用的交易所、运行模式）。

## 响应预期

在收到报告后我们会尽快确认、评估影响并给出的修复时间表。修复完成前建议保持关注，避免公开披露细节。

## 最低安全基线

部署到有真实资金的环境前，请至少满足以下基线：

- **默认以 `observe`（观察）模式运行**：仅解析与记录信号、不下单。只有在你明确确认配置无误后才切换到 `testnet`、`real`。
- **必须修改 `SERVER_JWT_SECRET`**：服务启动时会检测默认占位密钥并拒绝启动。生产环境请设置为高强度随机值。
- **设置 `SERVER_ENCRYPTION_KEY` 进行字段级 AES-256-GCM 加密**：通过该变量可对 `ExchangeInstance` 的 `apiKey` / `apiSecret` / `privateKey` 及 `AIConfig` 的 `apiKey` 等敏感字段进行字段级加密，避免交易所 / AI 密钥明文存储。未设置时以明文存储运行，并打印安全告警（向后兼容）。建议生产环境务必设置。
- **备份 / 恢复接口仅 admin 可用**：`/api/backup` 已启用 admin 权限校验，请勿将其暴露到公网，涉及导出的备份文件落在本地可信环境。
- **遵守最小权限**：仅向运行进程授予其必要的网络与文件访问权限，限制数据库文件（`DB_STORAGE`）的读写范围。
- **定期审计日志与账户操作**：关注 `ADMIN_DEFAULT_PASSWORD` 的初始密码，首次登录后尽快修改。